/**
 * HTML/CSS parsing + same-origin link discovery. Stateless functions that
 * turn raw markup into the structured shapes the pipeline consumes.
 */
export * from './parse.js';
export * from './crawl.js';
